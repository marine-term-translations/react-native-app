# .npmrc - npm configuration

# Use exact versions (no ^ or ~)
save-exact=true

# Increase timeout for slow networks
fetch-timeout=600000

# Use legacy peer deps for React Native
legacy-peer-deps=true
